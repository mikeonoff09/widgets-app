import 'package:flutter/material.dart';

class CardsScreeen extends StatelessWidget {
  const CardsScreeen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cards Screen'),
      ),
      body: const Placeholder(),
    );
  }
}
