import 'package:flutter/material.dart';

class ButtonsScreeen extends StatelessWidget {
  const ButtonsScreeen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Buttons Screen'),
      ),
      body: const Placeholder(),
    );
  }
}
